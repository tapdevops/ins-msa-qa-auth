/**
 * syncMobile
 * Untuk menyediakan data mobile
 * --------------------------------------------------------------------------
 */
exports.init = ( req, res ) => {
	res.json( {
		message: "Success! Config database berhasil dirubah."
	} );
}